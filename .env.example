# Optional external basemap. By default the presentation uses local hillshade.
VITE_MAP_STYLE_URL=
VITE_MAPTILER_KEY=
# The backend reads PORT and DATA_DIR from process environment variables.
# Default API port: 3001. Default storage: .data/scenes.
# If PORT changes, update the Vite proxy target too.
